
import server = require("../src/app/server");

describe("The thing", () => {
  it("should just work", () => {
    expect(true).toBe(true);
  });
  it("and not complain", () => {
    expect(true).toBe(false);
  });
});
