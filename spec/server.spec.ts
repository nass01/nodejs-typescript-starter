
import server = require("../src/app/server");

describe("The thing", () => {
  it("should just work", () => {
    expect(true).toBe(false);
  });
  it("and not complain", () => {
    expect(true).toBe(false);
  });
});
